name: ci

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run check
      - run: npm run build
      - run: npm run lint
      - run: npm test
      - name: Pre-PR bootstrap guard (fail closed)
        run: node scripts/pre-pr-bootstrap-guard.mjs --repo-dir .
